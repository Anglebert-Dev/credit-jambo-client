import { Router } from 'express';

const router = Router();

// TODO: Implement credit routes
// - POST /request
// - GET /requests
// - GET /requests/:id
// - POST /repay/:id
// - GET /repayments/:id

export default router;
